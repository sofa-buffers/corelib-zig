# Copy this file to .devcontainer/.env and fill in your values.
# The real .env holds secrets and is gitignored — never commit it.

# GitHub token used by the `gh` CLI inside the container.
# Required scopes: repo (push, PRs), read:org (repo listing), workflow (CI).
GH_TOKEN=
