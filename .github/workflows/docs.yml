name: Docs

on:
  push:
    branches: [main]
  workflow_dispatch:

# Least-privilege token plus the scopes the Pages deploy needs.
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one Pages deploy at a time; don't cancel an in-flight one.
concurrency:
  group: pages
  cancel-in-progress: false

# Both jobs are bounded in time. A hung step must fail fast and name itself:
# the account's Actions concurrency is 20, and one wedged job — held for
# GitHub's default six hours — is a slot everyone else waits on.
jobs:
  build:
    name: Build Zig autodoc
    runs-on: ubuntu-latest
    timeout-minutes: 10   # ~25 s observed
    steps:
      - uses: actions/checkout@v7
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      # Zig's built-in documentation generator (autodoc) over the `sofab` module.
      - name: Build documentation
        run: zig build docs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: zig-out/docs

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10   # ~10 s observed; the Pages API call is the only wait
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
